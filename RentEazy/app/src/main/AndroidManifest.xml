<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RentEazy"
        tools:targetApi="31">

        <!-- Définir SplashActivity comme activité principale -->

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.RentEazy">
            <intent-filter tools:ignore="ExtraText">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />

#Exclude everything
*
</intent-filter>
        </activity>
        <activity android:name=".WelcomeActivity" android:exported="true" android:theme="@style/Theme.RentEazy" />
        <activity android:name=".RegisterActivity" android:exported="true" android:theme="@style/Theme.RentEazy" />
        <activity android:name=".Login" android:exported="true" android:theme="@style/Theme.RentEazy" />
        <activity android:name=".MainActivity" android:exported="true" android:theme="@style/Theme.RentEazy" />
        <activity android:name=".HomeActivity" android:exported="true" android:theme="@style/Theme.RentEazy" />
        <activity android:name=".SettingsActivity" android:exported="true" android:theme="@style/Theme.RentEazy" />
        <activity android:name=".HomeBailleurActivity" android:exported="true" android:theme="@style/Theme.RentEazy" />
        <activity android:name=".ForgotPasswordActivity" android:exported="true" android:theme="@style/Theme.RentEazy" />
        <activity android:name=".DetailHouseActivity" android:exported="true" android:theme="@style/Theme.RentEazy" />
        <activity android:name=".ChatActivity" android:exported="true" android:theme="@style/Theme.RentEazy" />



    </application>

</manifest>